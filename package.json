{
  "name": "by-miles-leaderboard",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^5.9.4",
    "history": "^4.9.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-flip-move": "^3.0.3",
    "react-modal": "^3.8.1",
    "react-redux": "^7.0.2",
    "react-router-dom": "^5.0.0",
    "react-scripts": "2.1.8",
    "redux": "^4.0.1",
    "redux-thunk": "^2.3.0",
    "scss-loader": "0.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy:test": "NODE_ENV=test npm run build && firebase use test && firebase deploy && firebase use dev",
    "deploy:dev": "NODE_ENV=development npm run build && firebase use dev && firebase deploy && firebase use dev",
    "deploy:prod": "NODE_ENV=production npm run build && firebase use prod && firebase deploy && firebase use dev"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "node-sass": "^4.12.0"
  }
}
